org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
android.overridePathCheck=true
# Kotlin incremental compilation causes "Storage already registered" errors on Windows
# Disable it for stable builds
kotlin.incremental=false
kotlin.incremental.usePreciseJavaTracking=false

# mobile_scanner 7.2.0 已移除 Renderscript，bundled MLKit 即可
# (unbundled 版与 7.2.0 源码不兼容，会报 Barcode.data 找不到)
