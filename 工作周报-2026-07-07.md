# 远东工友App 工作周报

**周期**: 2026年6月29日（周一）— 7月7日（周二）
**项目状态**: 🟢 持续迭代中（本周完成多项体验优化）
**提交人**: AI 工作助手

---

## 一、本周工作概况

| 指标 | 数值 |
|------|------|
| 新增 Git Commit | 3 个（`LOGO` / `feat: 体验优化` / `CORS 白名单`） |
| 代码新增行 | ~533 行 |
| 代码删除行 | ~180 行 |
| 涉及文件 | ~66 个 |
| 未提交文件 | 7 个（Docker 部署 / 周报 / POM 修改等） |
| 后端变更 | 1 个（SecurityConfig CORS） |
| 前端变更 | >60 个 Dart/资源/配置文件 |

---

## 二、主要进展

### 2.1 🎨 品牌视觉统一（6/29）

**核心改动**：方案一→方案二切换，全面落地"远东幕墙"品牌

| 子项 | 详情 |
|------|------|
| **品牌色切换** | #1E3A5F 深蓝科技风 → #2563EB 明亮蓝 + 白色背景极简风 |
| **Logo 上架** | Android 5 个 mipmap + iOS 19 个 AppIcon，全部使用"远东幕墙"中文 Logo |
| **启动/登录页** | 白底 + 亮蓝光晕 + Logo 淡入动画 |
| **品牌组件** | `brand_logo.dart` 弧形穹顶 CustomPainter 更新配色引用 |
| **应用名称** | AndroidManifest / Info.plist 统一更新为「遠東工友通」 |
| **影响范围** | theme.dart / splash_page / login_page / worker_home_page 等 30+ 文件 |

### 2.2 📱 交互体验大提升（6/30 提交，7/3 落地）

| 功能 | 实现方式 | 影响端 |
|------|----------|--------|
| **左右滑动切换 Tab** | PageView + PageController 替代 IndexedStack | 工人/内部/判头三端首页 |
| **App 快捷操作** | quick_actions 插件 + ShortcutService | Android 长按图标弹出 |
| **震动反馈** | HapticFeedback.heavyImpact/mediumImpact | 打卡 / 审批 / 扫码 |
| **双击返回退出** | PopScope 包裹 + 2 秒内再按提示 | 三个首页 |
| **BottomNav 角标** | Badge 组件显示未读通知数 | 工人「我的」/ 内部「個人中心」 |
| **空状态插图** | EmptyStateWidget（入场动画+插图+文字+按钮） | 判头审核列表等 7 处 |

**新增依赖**：`quick_actions: ^1.1.0`

### 2.3 📦 APK 体积精简 & 构建加固（6/29）

| 措施 | 详情 |
|------|------|
| **ABI 拆分** | `--split-per-abi` 分架构打包，减少单包体积 |
| **R8 压缩** | `isMinifyEnabled=true` + `isShrinkResources=true` |
| **x86_64 剔除** | `--target-platform android-arm,android-arm64` |
| **mobile_scanner 升级** | 6.0.0 → 7.2.0（修复 Renderscript 弃用警告） |
| **构建错误修复（3项）** | `com.android.library` 缺失 / R8 Play Core / Kotlin 增量编译缓存损坏 |
| **ProGuard 规则** | 新建 `proguard-rules.pro`（含 Flutter/MLKit/Play Core） |

**关键技术发现**：
- MLKit unbundled 与 mobile_scanner 7.2.0 不兼容（`Barcode.data` API 已移除），只能使用 bundled
- Kotlin 增量编译在 Windows 上因内存映射文件并发冲突损坏，已禁用（`kotlin.incremental=false`）
- `ndk.abiFilters` 对 Flutter libapp.so 无效，必须用 `--target-platform`

### 2.4 🐳 Docker 容器化部署方案（6/29）

| 组件 | 详情 |
|------|------|
| **架构** | 3 容器：postgres:15-alpine + redis:7-alpine + backend（Spring Boot） |
| **构建** | 多阶段 Dockerfile（maven→JRE）+ 非 root 用户运行 |
| **网络** | 显式 bridge `fareast-net` (172.28.0.0/16) |
| **资源限制** | backend: 2CPU/2G, PG: 0.5CPU/256M, Redis: 0.25CPU/128M |
| **持久化** | 4 个 Volume：pgdata / redis-data / uploads-data / logs-data |
| **健康检查** | Actuator `/actuator/health` + 容器 HEALTHCHECK |
| **密钥管理** | 零硬编码，全部通过 `.env` → docker-compose 环境变量注入 |

**新增文件**：`backend/Dockerfile` / `deploy/docker-compose.yml` / `deploy/.env.example` / `deploy/README.md` / `deploy/init-scripts/` / `application-docker.yml`

### 2.5 ☁️ iOS CI 自动化打包（6/29）

| 组件 | 详情 |
|------|------|
| **GitHub Actions** | `ios-build.yml` on `develop`/`feature/*`/`refactor/*` push |
| **Fastlane** | Fastfile（build + TestFlight 双 lane）+ Appfile + Matchfile |
| **签名管理** | Fastlane Match 私有证书仓库（readonly 模式） |
| **工程配置** | `project.pbxproj` 补全 `CODE_SIGN_STYLE=Automatic` + DEVELOPMENT_TEAM |

**前提条件**：Apple Developer Program 账号 / Team ID / App Store Connect API Key / Match 仓库

### 2.6 📞 Twilio SMS 验证码开通指南（7/3）

为港澳及新加坡输出完整开通指南文档 `docs/Twilio_SMS_验证码服务开通指南.md`：

| 地区 | 重点 |
|------|------|
| **中国香港 (+852)** | Alphanumeric Sender ID 需预注册（约 18 天） |
| **中国澳门 (+853)** | 最简单，动态 Alphanumeric Sender ID 无需预注册 |
| **新加坡 (+65)** | 必须走 SSIR（SGNIC）注册，否则标记 "Likely-SCAM" |
| **推荐方案** | Twilio Verify API（$0.05/次 + 地区 SMS 费） |

### 2.7 🔐 CORS 白名单扩展（7/6）

在 `SecurityConfig.java` 中新增 `*.fefacade.com` 域名的 CORS 放行：

```java
"http://*.fefacade.com:*",
"https://*.fefacade.com:*"
```

适配测试和生产环境的前端部署域名。

### 2.8 📋 分判商季度安全健康考核评分（需求分析阶段，7/6）

> **⚠️ 尚未开发代码，仅完成方案设计**

| 项目 | 内容 |
|------|------|
| **来源** | `分判商季度安全健康表現評分.docx`（21 项评分表） |
| **评分结构** | 安全投放资源(8项) + 实地安环(10项) + 安全表现(3项)，单项 0-10 分，满分 210 |
| **阈值规则** | <80% 书面警告；<60% 或年度连续两季<80% → 不合格 |
| **审批流** | 安全人员(填报) → 项目经理(通过) → 分判商(知会) |
| **新增角色** | `SAFETY_OFFICER`（UserRole 枚举扩展） |
| **新增表** | 3 张：`subcontractor_safety_assessments` / `_items` / `unqualified_contractor_records` |

---

## 三、关键变更汇总

### 新增 Git Commit

```
c247f01 (7/6)  CORS 白名单
               └─ SecurityConfig.java — 新增 *.fefacade.com 域名放行

1a0bd8d (7/3)  feat: 体验优化
               ├─ lib/pages/worker/worker_home_page.dart — PageView 滑动 + 快捷功能
               ├─ lib/pages/contractor/ -> PageView 滑动
               ├─ lib/pages/internal/    -> PageView 滑动 + 个人中心
               ├─ lib/widgets/empty_state_widget.dart — 新组件
               ├─ lib/services/shortcut_service.dart — 长按快捷操作
               ├─ pubspec.yaml — 新增 quick_actions 依赖
               └─ 8 个 Android 快捷图标资源

d1a7b87 (6/29) LOGO
               ├─ assets/images/logo_*.png — 4 个 Logo 文件
               ├─ lib/config/theme.dart — 品牌色 #2563EB
               ├─ lib/pages/auth/login_page.dart — 新 Logo
               ├─ lib/pages/splash_page.dart — 白底+亮蓝淡入
               ├─ lib/widgets/brand_logo.dart — 弧形穹顶优化
               ├─ lib/main.dart / app.dart — 初始化调整
               ├─ Android mipmap-* — ic_launcher 全部更新
               ├─ iOS AppIcon — 19 个尺寸全部更新
               ├─ pubspec.yaml — Logo 资源注册
               └─ fastlane/ — CI 配置全套
```

### 未提交的工作树变更

```
后端:
  M pom.xml — 新增 actuator 依赖
  D uploads/videos/safety_01.mp4 — 视频已上传服务器

新增文件:
  backend/Dockerfile — 多阶段容器构建
  backend/application-docker.yml — Docker 专属配置
  deploy/ — 完整部署方案（docker-compose + 脚本 + README）
  .github/workflows/ — iOS CI 工作流 + README_SETUP.md
  .docx_content.txt — 评分需求解析中间文件
  工作周报-2026-07-03.md — 上周周报
```

---

## 四、待关注事项 ⚠️

### 🔴 P0 — 应尽快处理

| # | 事项 | 详情 |
|---|------|------|
| 1 | **README.md 合并冲突仍未解决** | HEAD（远东工友 App 版）vs remote（GitLab 模板版）的冲突从 6 月中旬延续至今，仓库主页展示异常 |
| 2 | **Docker 部署未实测** | docker-compose / Dockerfile / 环境变量全链路已规范设计，但未在目标服务器(10.106.0.242)启动验证 |
| 3 | **工作树变更待提交** | 7 个未追踪文件、2 个修改文件（pom.xml + safety_01.mp4 删除），多日工作存在丢失风险 |

### 🟡 P1 — 建议跟进

| # | 事项 | 详情 |
|---|------|------|
| 4 | **iOS CI 待触发验证** | Fastlane Match 首次运行需手动确认证书拉取正常，Apple Developer 账号权限待确认 |
| 5 | **Twilio 短信开通进度** | HK/MO/SG 三地资质申请流程已输出文档，实际开通需人工配合 |
| 6 | **分判商评分开发** | 方案设计已完成，预估工作量：后端 3 表 + 1 Controller + 1 Service；前端 SAFETY_OFFICER 全角色页面；待审核后开发 |

### 🔵 P2 — 可后续优化

| # | 事项 | 详情 |
|---|------|------|
| 7 | **ProGuard 规则过宽泛** | `-keep class io.flutter.** { *; }` 等规则导致 classes.dex 膨胀到 9.5MB，需要后续精简 |
| 8 | **backend 目录残留素材** | 68 个 PDF / 42 个 SQL / 16 个 JPG 原始素材在 backend 目录中，建议清理 |
| 9 | **潮汐并发/离线打卡** | 上期报告指出的架构隐患（无唯一约束、无分布式锁、无限流等）仍未解决 |

---

## 五、与上期待办对照

| 上期建议 | 本期进展 | 状态 |
|----------|----------|------|
| 修复 README.md 冲突 | ❌ 仍未解决 | 🔴 延滞 |
| 提交全部工作树变更 | ⚠️ 部分提交（LOGO + 体验优化），Docker 等仍待提交 | 🟡 部分完成 |
| 验证 Docker 部署 | ❌ 未实测 | 🔴 延滞 |
| 触发 iOS CI 测试 | ❌ 未触发 | 🔴 延滞 |
| 跟进 Twilio 开通 | ✅ 已输出完整开通指南 | 🟢 完成 |
| 回归测试核心功能 | ❌ 未执行 | 🔴 延滞 |

---

## 六、下周建议

| 优先级 | 建议 |
|--------|------|
| 🔴 1 | 修复 README.md 冲突并提交全部工作树变更 |
| 🔴 2 | 在目标服务器验证 Docker 容器化部署流程 |
| 🟡 3 | 触发 iOS CI 并确认 TestFlight 上传正常 |
| 🟡 4 | 推动分判商季度安全评分功能开发（先审方案后写代码） |
| 🟡 5 | 清理 backend/ 目录中的原始素材（PDF/SQL/JPG） |
| 🔵 6 | 研究潮汐并发防护方案（唯一约束 + 限流 + 分布式锁） |

---

*周报生成时间: 2026年7月7日 09:15 · 数据来源: Git log + 每日工作日志 + 工作树扫描 + conversation-log*
